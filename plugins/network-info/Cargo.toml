[package]
name = "network-info-plugin"
version = "1.0.0"
edition = "2021"

[lib]
name = "network_info_plugin"
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
strip = true
